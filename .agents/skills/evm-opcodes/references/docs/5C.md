---
fork: Cancun
group: Stack Memory Storage and Flow Operations
---

## Stack input

0. `key`: 32-byte key in [transient storage](./about.mdx).

## Stack output

0. `value`: 32-byte value corresponding to that key. 0 if that key was never written before.

## Examples

|Transient storage key | Transient storage value |
|-----------:|--------------:|
| `0` | `46` |

| * | Input | Output | * | * | Input | Output |
|--:|------:|-------:|--:|--:|------:|-------:|
| `1` | `0` | `46` | * | `1` | `1` | `0` |

## Error cases

The state changes done by the current context are [reverted](#FD) in those cases:
- Not enough gas.
- Not enough values on the stack.
- The current execution context is from a [STATICCALL](/#FA) (since Byzantium fork) or an [EXTSTATICCALL](/#FB) (since EOF fork).
