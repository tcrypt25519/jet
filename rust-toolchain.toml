[toolchain]
# Use stable Rust by default for building and testing
channel = "stable"

# Components needed for development
components = ["rustfmt", "clippy"]

# Specify the profile - minimal is faster to install
profile = "default"

# Note: For formatting and linting in CI, we use nightly explicitly
# Install nightly with: rustup toolchain install nightly --component rustfmt,clippy
